lanes:
  pr_fast:
    include: [smoke]
    maxDurationMin: 10
  merge:
    include: [critical]
    addImpactedRegression: true
  nightly_deep:
    include: [regression]
    includeReliabilityAnalytics: true

riskWeights:
  changedCriticalDomain: 5
  recentProdIncident: 4
  highDefectDensity: 3

thresholds:
  high: 7
  medium: 4
  low: 0
